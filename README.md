# RidePulse 🚴

Система мониторинга метрик велосипедистов в реальном времени.

## Обзор

RidePulse собирает, обрабатывает и отображает данные с датчиков:
- **Пульс** (Heart Rate)
- **Мощность** (Power)
- **Каденс** (Cadence)
- **Скорость** (Speed)
- **GPS** координаты

## Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                        Клиентские приложения                    │
├─────────────────────────────────────────────────────────────────┤
│  Mobile Rider (Android)    │  Mobile Coach (Android)            │
│  - BLE сенсоры             │  - Мониторинг райдеров            │
│  - Отправка метрик         │  - Просмотр алертов                │
│  - Роль: Rider             │  - Роль: Coach / Admin             │
└───────────────────────────┴────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                      API Gateway (Port 3000)                    │
│  - Аутентификация (JWT)                                         │
│  - Авторизация по ролям                                         │
│  - Маршрутизация запросов                                       │
└─────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    ▼                               ▼
┌───────────────────────────────┐  ┌──────────────────────────────┐
│   Ingest WebSocket (8080)     │  │   Metrics Processor (8081)   │
│   - Приём метрик по WS        │  │   - Обработка метрик         │
│   - Pub/Sub в Redis           │  │   - Сохранение в PostgreSQL  │
│   - Broadcast тренерам        │  │   - Агрегация данных         │
└───────────────────────────────┘  └──────────────────────────────┘
                    │                               │
                    └───────────────┬───────────────┘
                                    ▼
                         ┌──────────────────────┐
                         │   Alert Engine (8082)│
                         │   - Проверка порогов │
                         │   - Генерация алертов│
                         │   - Подтверждение    │
                         └──────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    ▼                               ▼
              ┌──────────┐                    ┌──────────┐
              │  Redis   │                    │PostgreSQL│
              │  Pub/Sub │                    │  Хранилище│
              └──────────┘                    └──────────┘
```

## Структура проекта

```
ridepulse/
├── apps/                          # Клиентские приложения
│   ├── mobile-rider/              # Android приложение для велогонщиков
│   ├── mobile-coach/              # Android приложение для тренеров
│   └── web-dashboard/             # Веб-панель для аналитики
├── services/                      # Микросервисы
│   ├── api-gateway/               # API Gateway (аутентификация)
│   ├── ingest-ws/                 # WebSocket сервис для приёма метрик
│   ├── metrics-processor/         # Обработка метрик
│   ├── gps-processor/            # Обработка GPS данных
│   ├── alert-engine/              # Движок алертов
│   └── replay-service/            # Сервис реплеев заездов
├── libs/                          # Общие библиотеки
│   ├── proto/                     # Protobuf определения (в планах)
│   ├── shared-types/              # Общие типы данных
│   ├── ui-kit/                    # UI компоненты (в планах)
│   └── utils/                     # Утилиты (в планах)
├── infra/                         # Инфраструктура
│   ├── docker/                    # Docker конфигурации
│   ├── k8s/                       # Kubernetes манифесты
│   └── terraform/                 # Terraform конфигурации
├── scripts/                       # Скрипты CI/CD
└── docs/                          # Документация
```

## Ролевая модель

### Rider (Велогонщик)
- Подключение BLE сенсоров
- Отправка метрик на сервер
- Просмотр своих метрик
- Начало/завершение сессии

### Coach (Тренер)
- Просмотр всех райдеров команды
- Real-time мониторинг метрик
- Просмотр и подтверждение алертов
- Доступ к аналитике и истории

### Admin (Администратор)
- Все права тренера
- Управление пользователями
- Управление командами
- Настройка порогов алертов

## Требования

### Backend
- Node.js 18+
- PostgreSQL 14+
- Redis 7+
- Docker (опционально)

### Frontend
- React 18+
- Next.js 14+
- Node.js 18+

### Mobile
- Android Studio Hedgehog+
- JDK 17
- Android SDK 34

## Быстрый старт

### 1. Запуск инфраструктуры с Docker

```bash
# PostgreSQL
docker run -d \
  --name ridepulse-postgres \
  -e POSTGRES_DB=ridepulse \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=postgres \
  -p 5432:5432 \
  postgres:14

# Redis
docker run -d \
  --name ridepulse-redis \
  -p 6379:6379 \
  redis:7-alpine
```

### 2. Сборка shared-types

```bash
cd ridepulse/libs/shared-types
npm install
npm run build
```

### 3. Запуск backend сервисов

```bash
# API Gateway
cd ridepulse/services/api-gateway
npm install
npm run dev

# Ingest WebSocket (в другом терминале)
cd ridepulse/services/ingest-ws
npm install
npm run dev

# Metrics Processor (в другом терминале)
cd ridepulse/services/metrics-processor
npm install
npm run dev

# Alert Engine (в другом терминале)
cd ridepulse/services/alert-engine
npm install
npm run dev
```

### 4. Запуск Web Dashboard

```bash
cd ridepulse/apps/web-dashboard
npm install
npm run dev
```

Откройте http://localhost:3000

### 5. Запуск Mobile Rider

```bash
cd ridepulse/apps/mobile-rider
./gradlew assembleDebug
./gradlew installDebug
```

### 6. Запуск Mobile Coach

```bash
cd ridepulse/apps/mobile-coach
./gradlew assembleDebug
./gradlew installDebug
```

## Конфигурация

### Переменные окружения

Создайте `.env` файл для каждого сервиса:

**API Gateway (.env)**
```env
PORT=3000
JWT_SECRET=your-secret-key
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=ridepulse
PG_USER=postgres
PG_PASSWORD=postgres
ADMIN_EMAIL=admin@ridepulse.com
ADMIN_PASSWORD=admin123
CORS_ORIGIN=*
```

**Ingest WebSocket (.env)**
```env
PORT=8080
REDIS_URL=redis://localhost:6379
JWT_SECRET=your-secret-key
CORS_ORIGIN=*
```

**Metrics Processor (.env)**
```env
PORT=8081
REDIS_URL=redis://localhost:6379
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=ridepulse
PG_USER=postgres
PG_PASSWORD=postgres
```

**Alert Engine (.env)**
```env
PORT=8082
REDIS_URL=redis://localhost:6379
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=ridepulse
PG_USER=postgres
PG_PASSWORD=postgres
```

**Web Dashboard (.env.local)**
```env
NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws
NEXT_PUBLIC_API_URL=http://localhost:3000
```

## API endpoints

### Аутентификация
- `POST /api/auth/register` — Регистрация
- `POST /api/auth/login` — Вход
- `POST /api/auth/refresh` — Обновление токена
- `GET /api/auth/me` — Текущий пользователь

### Райдеры
- `GET /api/riders` — Список райдеров (Coach/Admin)
- `GET /api/riders/:id` — Детали райдера

### Сессии
- `GET /api/sessions/active` — Активные сессии
- `GET /api/sessions/:id` — Детали сессии
- `GET /api/sessions/:id/metrics` — Метрики сессии

### Алерты
- `GET /api/alerts/:riderId` — Алерты райдера
- `PUT /api/alerts/:alertId/acknowledge` — Подтверждение алерта
- `GET /api/thresholds/:riderId` — Пороги райдера
- `PUT /api/thresholds/:riderId` — Обновление порогов

### GPS
- `GET /api/gps/tracks/:sessionId` — GPS трек сессии
- `POST /api/gps/segments/:sessionId/create` — Создание сегментов
- `GET /api/gps/segments/:sessionId` — Сегменты сессии

### Replay
- `GET /api/sessions` — Список сессий для реплея
- `GET /api/sessions/:id` — Детали сессии

## WebSocket события

### Клиент → Сервер
- `sensor_data` — Отправка метрик (Rider)
- `session_start` — Начало сессии (Rider)
- `session_end` — Завершение сессии (Rider)
- `acknowledge_alert` — Подтверждение алерта (Coach/Admin)
- `subscribe_rider` — Подписка на райдера (Coach/Admin)
- `unsubscribe_rider` — Отписка от райдера (Coach/Admin)

### Сервер → Клиент
- `rider_metrics` — Обновление метрик райдера
- `alert` — Новый алерт
- `session_start` — Начало сессии
- `session_end` — Конец сессии
- `active_sessions` — Список активных сессий

## Поддерживаемые сенсоры

### Bluetooth LE (BLE)
- **Heart Rate** — стандартный сервис `0x180D`
- **Cycling Power** — стандартный сервис `0x1818`
- **Cycling Speed and Cadence** — стандартный сервис `0x1816`

## Разработка

### Линтеринг

```bash
# Backend
npm run lint

# Web Dashboard
npm run lint

# Mobile
./gradlew lint
```

### Проверка типов

```bash
# Backend
npm run type-check

# Web Dashboard
npm run type-check

# Mobile
./gradlew compileDebugKotlin
```

## Тестирование

### Backend
```bash
npm test
```

### Web Dashboard
```bash
npm test
```

### Mobile
```bash
./gradlew test
```

## Документация

- [Mobile Rider README](apps/mobile-rider/README.md)
- [Mobile Coach README](apps/mobile-coach/README.md)
- [Web Dashboard README](apps/web-dashboard/README.md)

## Лицензия

MIT

## Контакты

Для вопросов и предложений создайте issue в репозитории.
