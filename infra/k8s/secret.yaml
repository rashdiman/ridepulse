apiVersion: v1
kind: Secret
metadata:
  name: ridepulse-secrets
  namespace: ridepulse
type: Opaque
stringData:
  # JWT Secret
  jwt-secret: "your-secret-key-change-this-in-production"
  
  # PostgreSQL
  pg-user: "postgres"
  pg-password: "postgres"
  pg-database: "ridepulse"
  
  # Admin credentials
  admin-email: "admin@ridepulse.com"
  admin-password: "admin123"
  
  # Redis
  redis-password: ""
